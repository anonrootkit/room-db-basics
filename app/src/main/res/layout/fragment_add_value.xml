<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <EditText
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_margin="24dp"
            android:id="@+id/value_edit_text"
            android:hint="Enter anything here..." />

        <com.google.android.material.button.MaterialButton
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/value_edit_text"
            app:layout_constraintStart_toStartOf="@id/value_edit_text"
            app:layout_constraintEnd_toEndOf="@id/value_edit_text"
            android:layout_marginTop="16dp"
            android:id="@+id/add_value_button"
            android:text="Add value to DB" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>